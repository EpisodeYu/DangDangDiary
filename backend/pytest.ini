[pytest]
asyncio_mode = auto
markers =
    integration: requires postgres container (skipped in default SQLite runs)
# NOTE: Chunk A-1 ships without strict DeprecationWarning promotion.
# Chunk B-1 (`datetime.utcnow` replacement) will turn this on:
#   filterwarnings = error::DeprecationWarning
